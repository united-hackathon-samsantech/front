import { FunnelStep } from "@/types";

interface CompleteProps extends FunnelStep {}

const Complete = ({ nextStep, prevStep }: CompleteProps) => {
  return <div>hi</div>;
};

export default Complete;
