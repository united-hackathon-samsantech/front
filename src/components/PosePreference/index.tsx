import { FunnelStep } from "@/types";

interface PostPreferenceProps extends FunnelStep {}

const PostPreference = ({ nextStep, prevStep }: PostPreferenceProps) => {
  return <div>hi</div>;
};

export default PostPreference;
