import { FunnelStep } from "@/types";

interface CapturePhoto extends FunnelStep {}

const CapturePhoto = ({ nextStep, prevStep }: CapturePhoto) => {
  return <div>hi</div>;
};

export default CapturePhoto;
